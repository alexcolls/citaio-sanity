import React from 'react';
import { DocumentDiff } from '../../../types';
declare type Props = {
    diffs: DocumentDiff[][];
    onConfirm?: (diffs: DocumentDiff[][]) => Promise<void>;
    onClose?: () => void;
};
export declare const MaintenanceTabDiffDialog: React.FC<Props>;
export {};
